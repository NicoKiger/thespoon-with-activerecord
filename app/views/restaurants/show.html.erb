<h1><%= @restaurant.name %></h1>

<ul>
  <li>Address: <strong><%= @restaurant.address %></strong></li>
  <li>Rating: <%= "★" * @restaurant.rating %><%= "☆" * (5 - @restaurant.rating) %></li>
</ul>

<p><%= link_to "Edit", edit_restaurant_path(@restaurant) %></p>

<p>⚠️<%= link_to "Delete #{@restaurant.name}", restaurant_path(@restaurant), method: :delete, data: { confirm: "Are you sure?" } %></p>
